from django.apps import AppConfig


class DiariesConfig(AppConfig):
    """
    Django App Config for the "diaries" app.
    """

    default_auto_field = "django.db.models.BigAutoField"
    name = "diaries"
